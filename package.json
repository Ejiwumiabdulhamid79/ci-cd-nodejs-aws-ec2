{
  "name": "nodejs-app",
  "version": "0.0.1",
  "description": "simple new app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"  //  <--  Express should be here (version may vary)
  },
}
